# Local development only — real values live in SSM Parameter Store (see template.yaml)
TABLE_NAME=YouEnjoyMyFamily-dev
STAGE=dev

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/oauth/google/callback

# Grocery cart items are tagged with a store (giant_eagle | aldi) and kept as
# a plain list — neither retailer exposes a public product/stock API, so
# there's no credential to configure here.
